# WorkBuddy 协作入口

将本文件或以下路径配置为工作区长期说明入口：

```text
{{MEMORY_PATH}}/README.md
```

1. 按 `Memory/README.md` 的顺序读取与当前任务有关的记忆。
2. 默认只读；只有用户明确授权后才写入 `Memory/INBOX/`。
3. 不复制一套独立主记忆，不用旧记录覆盖较新的确认记录。
4. 不读取、保存或复述凭据、联系方式、完整地址或不必要的账号信息。

本文件只是配置入口；除非已经在产品内验证，否则不要声称 WorkBuddy 会自动加载它。
