# Hermes 协作入口

1. 完整阅读 `{{MEMORY_PATH}}/README.md`。
2. 按其中顺序读取与当前任务有关的记忆；更近层级的项目规则在其作用域内优先。
3. 长期记忆默认只读；写入候选区和合并主记忆分别需要明确授权。
4. 不复制或重建第二套主记忆，不用旧记录覆盖较新的确认记录。
5. 不读取、复述或保存密码、Token、API Key、Cookie、SSH 私钥、`.env`、`.dev.vars` 等敏感内容。
6. 不擅自覆盖、删除、上传、Git commit 或 push。

不要猜测或编造工具输出、文件内容或加载状态；无法验证时明确说明。
